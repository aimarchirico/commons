version: '3'

dotenv: ['.env']

includes:
  maven:
    taskfile: ./maven/Taskfile.yml
    dir: ./maven
  npm:
    taskfile: ./npm/Taskfile.yml
    dir: ./npm
  tools:
    taskfile: ./tools/Taskfile.yml
    dir: ./tools
    flatten: true

tasks:
  default:
    cmds:
      - task --list
