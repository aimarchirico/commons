name: Close Sub-Issues
description: Cascade close every nested sub-issue
inputs:
  token:
    description: Token used to read and close sub-issues
    required: false
    default: ${{ github.token }}
runs:
  using: composite
  steps:
    - name: Close sub-issues
      uses: actions/github-script@v7
      with:
        github-token: ${{ inputs.token }}
        script: |
          // BFS to close all nested sub-issues
          const queue = [context.payload.issue.number];
          while (queue.length) {
            const issue_number = queue.shift();
            const subIssues = await github.paginate(
              "GET /repos/{owner}/{repo}/issues/{issue_number}/sub_issues",
              { ...context.repo, issue_number, per_page: 100 }
            );
            for (const sub of subIssues) {
              if (sub.state === "open") {
                await github.rest.issues.update({
                  ...context.repo,
                  issue_number: sub.number,
                  state: "closed",
                });
                core.info(`Closed sub-issue #${sub.number}`);
              }
              queue.push(sub.number);
            }
          }
