version: '3'

tasks:
  build:
    desc: Build test library module
    cmds:
      - bash ../gradlew :test:build
  check:
    desc: Run tests and quality checks for test module
    cmds:
      - bash ../gradlew :test:check
  fix:
    desc: Format test module code
    cmds:
      - bash ../gradlew :test:ktfmtFormat
