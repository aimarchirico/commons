[project]
name = "plugin-shared"
version = "1.0.0"
description = "PR review/thread/comment feedback logic shared across skills."
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/plugin_shared"]
