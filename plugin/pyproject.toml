[project]
name = "plugin"
version = "1.0.0"
requires-python = ">=3.13"
dependencies = [
  "commons-python",
]

[tool.uv]
package = false

[tool.uv.sources]
commons-python = { path = "../python/commons-python" }
